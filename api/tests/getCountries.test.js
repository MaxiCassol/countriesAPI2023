// const { getAllCountries } = require('../src/controllers/getCountries');
// const { Country } = require('../src/models');
// const expectedResponse = require('./data/countries.json');

// jest.mock('../models');

// describe('getAllCountries', () => {
//   test('should return all countries with activities', async () => {
//     // Simulamos la consulta a la base de datos
//     Country.findAll.mockResolvedValue(expectedResponse);

//     // Ejecutamos la función getAllCountries
//     const actualResponse = await getAllCountries();

//     // Comparamos la respuesta esperada con la respuesta actual
//     expect(actualResponse).toEqual(expectedResponse);
//   });
// });
